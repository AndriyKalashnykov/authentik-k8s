kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
# Single control-plane node is enough for the Authentik e2e. LoadBalancer
# Services are served by cloud-provider-kind on the host `kind` Docker network.
nodes:
  - role: control-plane
